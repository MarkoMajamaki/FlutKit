name: flutkit_demo
description: Demo and testing app for flutkit
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  flutkit:
    path: ../flutkit

dev_dependencies:
  flutter_test:
    sdk: flutter
  provider: ^5.0.0

flutter:
  uses-material-design: true